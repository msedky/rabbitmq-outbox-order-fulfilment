# Warehouse Service — Testing

## Overview

This document describes the unit and integration tests for the warehouse-service,
covering service layer, messaging layer, scheduler, and controller.

---

## Unit Tests

---

### StockServiceImplTest

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/service/impl/StockServiceImplTest.java`

**Framework:** JUnit 5 · Mockito · AssertJ

**Approach:** Repositories and `OutboxEventService` are mocked. `StockMapper` uses
the real `StockMapperImpl` generated by MapStruct to verify mapping correctness.

#### Create Stock Tests

| Test | Description |
|---|---|
| `testCreateStockSuccess` | Creates stock with valid input — verifies all response fields and repository save call |
| `testCreateStockWithZeroQuantity` | Creates stock with zero available quantity — verifies zero is handled correctly |

#### Get All Stocks Tests

| Test | Description |
|---|---|
| `testGetAllStocksSuccess` | Returns multiple stocks — verifies size, productIds, and quantities |
| `testGetAllStocksEmpty` | Returns empty list when no stocks exist |

#### Reserve Stock Tests

| Test | Description |
|---|---|
| `testReserveStockSuccessWithSingleItem` | Reserves stock for a single item — verifies quantity adjustments, reservation creation, and outbox event |
| `testReserveStockSuccessWithMultipleItems` | Reserves stock for 3 items — verifies each product is processed correctly |
| `testReserveStockInsufficientQuantity` | Throws `InsufficientStockException` when requested quantity exceeds available |
| `testReserveStockNotFound` | Throws `StockNotFoundException` when product does not exist |

#### Release Stock Tests

| Test | Description |
|---|---|
| `testReleaseStockSuccess` | Releases reserved stock — verifies quantities restored and status changed to `RELEASED` |
| `testReleaseStockNoReservationsFound` | Handles gracefully when no `RESERVED` reservations found for order |
| `testReleaseStockSuccessWithMultipleItems` | Releases 3 reservations — verifies each product's quantities restored correctly |
| `testReleaseStockStockNotFound` | Throws `StockNotFoundException` when stock entity not found during release |

#### Fulfill Stock Tests

| Test | Description |
|---|---|
| `testFulfillStockSuccess` | Fulfills reservation on `ShipmentOutForDeliveryEvent` — verifies `reservedQuantity` decremented and status set to `FULFILLED` |
| `testFulfillStockNoReservationsFound` | Handles gracefully when no `RESERVED` reservations found for order |
| `testFulfillStockMultipleItems` | Fulfills 2 reservations — verifies both stock quantities and statuses updated correctly |
| `testFulfillStockStockNotFound` | Throws `StockNotFoundException` when stock entity not found during fulfill |

---

### OutboxEventServiceImplTest

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/service/impl/OutboxEventServiceImplTest.java`

**Framework:** JUnit 5 · Mockito · AssertJ · ArgumentCaptor

**Approach:** `OutboxEventRepository` and `ObjectMapper` are mocked.
`ArgumentCaptor` is used to capture and assert the saved entity since it is built inside the method.

| Test | Description |
|---|---|
| `testSaveEventSuccess` | Saves outbox event with correct fields and `PENDING` status |
| `testSaveEventSerializationFailure` | Throws `IllegalStateException` when payload serialization fails — verifies repository is never called |
| `testGetPendingEvents` | Returns list of `PENDING` events from repository |
| `testMarkAsPublished` | Sets status to `PUBLISHED` and populates `publishedAt` |
| `testMarkAsFailed` | Sets status to `FAILED` |

---

### OutboxSchedulerTest

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/scheduler/OutboxSchedulerTest.java`

**Framework:** JUnit 5 · Mockito

**Approach:** `OutboxEventService` and `OutboxEventPublisher` are mocked.

| Test | Description |
|---|---|
| `testProcessPendingEventsWhenNoPendingEvents` | Does nothing when no pending events exist — publisher never called |
| `testProcessPendingEventsSuccessfully` | Publishes event and marks it as `PUBLISHED` |
| `testProcessPendingEventsWhenPublishingFails` | Marks event as `FAILED` when publisher throws exception |
| `testProcessPendingEventsContinuesAfterFailure` | Continues processing remaining events after one failure — failed event marked `FAILED`, successful event marked `PUBLISHED` |

---

### OutboxEventPublisherImplTest

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/messaging/impl/OutboxEventPublisherImplTest.java`

**Framework:** JUnit 5 · Mockito · AssertJ

**Approach:** Strategy map is injected with a mocked `OutboxEventPublishStrategy`.

| Test | Description |
|---|---|
| `testPublishWithMatchingStrategy` | Delegates to correct strategy based on event type |
| `testPublishWhenStrategyNotFound` | Throws `IllegalStateException` when no strategy found for event type |

---

### OrderEventConsumerTest

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/messaging/OrderEventConsumerTest.java`

**Framework:** JUnit 5 · Mockito

**Approach:** `StockService` and `Channel` are mocked. `maxRetries` is injected
via `ReflectionTestUtils`. `x-death` header is simulated via a helper method.

#### Handle Order Placed Tests

| Test | Description |
|---|---|
| `testHandleOrderPlacedSuccess` | Reserves stock and acks message |
| `testHandleOrderPlacedInsufficientStock` | Rejects without requeue on `InsufficientStockException` |
| `testHandleOrderPlacedStockNotFound` | Rejects without requeue on `StockNotFoundException` |
| `testHandleOrderPlacedRetryableFailureBeforeMaxRetries` | Rejects with requeue when retry count is below max |
| `testHandleOrderPlacedMaxRetriesReached` | Rejects without requeue when retry count reaches max |

#### Handle Order Cancelled Tests

| Test | Description |
|---|---|
| `testHandleOrderCancelledSuccess` | Releases stock and acks message |
| `testHandleOrderCancelledStockNotFound` | Rejects without requeue on `StockNotFoundException` |
| `testHandleOrderCancelledRetryableFailureBeforeMaxRetries` | Rejects with requeue when retry count is below max |
| `testHandleOrderCancelledMaxRetriesReached` | Rejects without requeue when retry count reaches max |

#### Handle Shipment Out For Delivery Tests

| Test | Description |
|---|---|
| `testHandleShipmentOutForDeliverySuccess` | Fulfills stock and acks message |
| `testHandleShipmentOutForDeliveryStockNotFound` | Rejects without requeue on `StockNotFoundException` |
| `testHandleShipmentOutForDeliveryRetryableFailureBeforeMaxRetries` | Rejects with requeue when retry count is below max |
| `testHandleShipmentOutForDeliveryMaxRetriesReached` | Rejects without requeue when retry count reaches max |

---

## Integration Tests — StockControllerIT

**Location:**
`src/test/java/com/rabbitmqoutbox/warehouseservice/controller/StockControllerIT.java`

**Framework:** JUnit 5 · Spring Boot Test · MockMvc · Testcontainers · AssertJ · Hamcrest

**Infrastructure:** PostgreSQL 16 Alpine and RabbitMQ 3 containers via Testcontainers.
Database is recreated fresh for each test run using `ddl-auto: create-drop`.
Each test starts with a clean state via `stockRepository.deleteAll()` in `@BeforeEach`.

### POST /api/v1/stocks — Create Stock Tests

| Test | Description |
|---|---|
| `testCreateStockSuccess` | Creates stock with valid request — verifies HTTP 201, full response fields, and DB persistence |
| `testCreateStockDuplicateProductId` | Returns HTTP 400 with `DUPLICATE_ENTRY` code when `productId` already exists |
| `testCreateStockWithZeroQuantity` | Creates stock with zero quantity — verifies HTTP 201 and DB persistence |
| `testCreateStockWithLargeQuantity` | Creates stock with quantity 999999 — verifies HTTP 201 and DB persistence |
| `testCreateStockMissingProductId` | Returns HTTP 400 when `productId` is blank |
| `testCreateStockMissingProductName` | Returns HTTP 400 when `productName` is blank |
| `testCreateStockMissingQuantity` | Returns HTTP 400 when `availableQuantity` is missing from request body |
| `testCreateStockNegativeQuantity` | Returns HTTP 400 when `availableQuantity` is negative |
| `testCreateStockInvalidJson` | Returns HTTP 400 when request body is malformed JSON |
| `testCreateStockMissingContentType` | Returns HTTP 415 when `Content-Type` header is missing |

### GET /api/v1/stocks — Get All Stocks Tests

| Test | Description |
|---|---|
| `testGetAllStocksEmpty` | Returns HTTP 200 with empty array when no stocks exist |
| `testGetAllStocksSuccess` | Returns HTTP 200 with all stocks — verifies size, all response fields for each item, and DB persistence for each returned stock |

---

## API Response Structure

### Success (200 / 201)
```json
{
  "success": true,
  "data": {
    "stockId": "uuid",
    "productId": "string",
    "productName": "string",
    "availableQuantity": 100,
    "reservedQuantity": 0,
    "createdAt": "2026-04-24T10:00:00Z",
    "updatedAt": "2026-04-24T10:00:00Z"
  },
  "error": null
}
```

### Error (400 / 415 / 500)
```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "error details",
    "timestamp": "2026-04-24T10:00:00Z"
  }
}
```

---

## Running the Tests

Run all tests:
```bash
mvn clean test
```

Run specific unit test class:
```bash
mvn clean test -Dtest=StockServiceImplTest
mvn clean test -Dtest=OutboxEventServiceImplTest
mvn clean test -Dtest=OutboxSchedulerTest
mvn clean test -Dtest=OutboxEventPublisherImplTest
mvn clean test -Dtest=OrderEventConsumerTest
```

Run integration tests only:
```bash
mvn clean test -Dtest=StockControllerIT
```

> Docker must be running for integration tests — Testcontainers requires it to start
> the PostgreSQL and RabbitMQ containers.
